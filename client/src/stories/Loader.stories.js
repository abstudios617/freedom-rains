import React from 'react';
import Loader from '../components/loader';

export default {
  title: 'Animation/Loader Component',
  component: Loader,
};

export const LoaderComponent = <Loader />;
